# Hello world ApiGateway and AWS Lambda API with Terraform

It creates:
- Nodejs Lambda function
- ApiGateway to invoke the function with CORS enabled for that endpoint

### To deploy run
`terraform apply`