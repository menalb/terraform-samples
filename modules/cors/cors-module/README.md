# ApiGateway CORS module

Add OPTIONS method and related configuration

## Parameters
`resource_id = aws_api_gateway_resource.[ api resource endpoint ].id`
`api_id      = aws_api_gateway_rest_api.[ api ].id`